<p id="notice"><%= notice %></p>
<h3>created events:</h3>
<ul>
<%= render partial: "user_events", collection: @created_events, :as => :event %>
</ul>

<h3>past events:</h3>
<ul>
<%= render partial: "user_events", collection: @past_events, :as => :event %>
</ul>


<h3>upcoming events:</h3>
<ul>
	<%= render partial: "user_events", collection: @upcoming_events, :as => :event %>
</ul>


<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>

